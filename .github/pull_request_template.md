## Result

<!-- State the delivered outcome and stop line. -->

## Documentation impact

- Classification: `none | technical | developer-site | generated | architecture-decision` (select all that apply)
- Reason:
- Authoritative source changed first:
- Core technical documents / developer projections / generated outputs updated:
- Required-target waivers (exact existing paths only) and reason:
- `core-docs-governance impact`, static check and affected docs build result:
- Code comments: unchanged unless the task explicitly owns the post-convergence comment phase.

## Verification

<!-- List only checks actually run and relevant checks explicitly deferred. -->
