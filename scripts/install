#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

exec php -r '
require "vendor/autoload.php";
exit(PeanutAdmin\App\command\InstallCli::run(getcwd(), array_slice($argv, 1)));
' -- "$@"
