#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

php vendor/bin/phpunit tests/recovery/RecoveryScriptContractTest.php
./scripts/verify-clean-install
./scripts/verify-recovery
./scripts/verify-internal-starter

echo 'Recovery, clean-install, and internal starter tests: OK'
